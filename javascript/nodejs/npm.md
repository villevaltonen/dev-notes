## NPM

```bash
# Creates package.json (manifest) files, list dependencies
npm init

# Install packge locally and add to package.json
npm install <package name> --save

# Install package globally
npm install <package name> -g

# Use it only in development
npm install <package nam> --save-dev
```